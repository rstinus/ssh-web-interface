{
  "name": "ssh-web-interface",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd frontend && npm install && npm run build",
    "start": "node backend/index.js",
    "start:backend": "cd backend && node index.js",
    "start:frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
